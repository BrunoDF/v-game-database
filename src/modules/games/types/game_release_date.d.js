import './game.d'

/**
 * @typedef GameReleaseDate
 * @property {number} id
 * @property {Game[]} game
 * @property {number} game.id
 * @property {string} game.name
 * @property {number} game.cover.image_id
 * @property {number} date
 */